[project]
name = "local-ai-mcp"
version = "0.1.0"
description = "Servidor MCP que expõe um modelo local (Ollama) como ferramentas para o Claude Code"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.2.0",
    "httpx>=0.27",
]
