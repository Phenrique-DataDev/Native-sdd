#!/usr/bin/env bash
# apply.sh (macOS) — STUB. Estrutura pronta para implementação futura (D6).
# Plano: instalar deps via Homebrew + montar ~/.claude por descoberta dinâmica
# (espelhar templates/global-claude), idempotente, com backup.
set -euo pipefail
echo "[INFO] Instalador macOS ainda não implementado (stub). Ver DESIGN_INSTALADOR.md."
exit 2
