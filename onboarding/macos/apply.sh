#!/usr/bin/env bash
# apply.sh (macOS) — STUB. Estrutura pronta para implementação futura (D6).
# Plano: instalar deps via Homebrew + montar ~/.claude por descoberta dinâmica
# (espelhar templates/global-claude), idempotente, com backup.
set -euo pipefail
echo "[INFO] Instalador macOS ainda não implementado (stub). Hoje só o Windows é suportado:"
echo "       use onboarding/install.ps1 (pwsh). Detalhes em onboarding/README.md."
exit 2
