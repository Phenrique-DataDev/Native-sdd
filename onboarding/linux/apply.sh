#!/usr/bin/env bash
# apply.sh (Linux) — STUB. Estrutura pronta para implementação futura (D6).
# Plano: instalar deps via apt/dnf/pacman (auto-detect) + montar ~/.claude por
# descoberta dinâmica (espelhar templates/global-claude), idempotente, com backup.
set -euo pipefail
echo "[INFO] Instalador Linux ainda não implementado (stub). Ver DESIGN_INSTALADOR.md."
exit 2
